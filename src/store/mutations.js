export default {
  SET_USERMSG(state, type) {
    state.userMsg = type;
  },

  SET_USERLIST(state, type) {
    state.userList = type;
  },

  SET_ENGINE(state, type) {
    state.engine = type;
  },

  SET_DOC(state, type) {
    state.doc = type;
  },

  SET_CH(state, type) {
    state.CH = type;
  },

  SET_NATURELIST(state, type) {
    state.natureList = type;
  },

  SET_NAVID(state, type) {
    state.navId = type;
  },

  SET_CMSG(state, type) {
    state.cMsg = type;
  },

  SET_VMSG(state, type) {
    state.vMsg = type;
  },

  SET_CARR(state, type) {
    state.cArr = type;
  },

  SET_SELEMS(state, type) {
    state.sElems = type;
  },

  SET_TELEMS(state, type) {
    state.tElems = type;
  },

  SET_HITARR(state, type) {
    state.hitArr = type;
  },

  SET_NATURESTATUS(state, type) {
    state.natureStatus = type;
  },
}
