export default {
  SET_CH(state, type) {
    state.CH = type;
  },

  SET_NAVID(state, type) {
    state.navId = type;
  },

  SET_CMSG(state, type) {
    state.cMsg = type;
  },

  SET_VMSG(state, type) {
    state.vMsg = type;
  },

  SET_CARR(state, type) {
    state.cArr = type;
  },

  SET_SELEMS(state, type) {
    state.sElems = type;
  },

  SET_TELEMS(state, type) {
    state.tElems = type;
  },
}
