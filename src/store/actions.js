export const setUserMsg = ({ commit }, type) => {
  commit('SET_USERMSG', type);
};

export const setUserList = ({ commit }, type) => {
  commit('SET_USERLIST', type);
};

export const setEngine = ({ commit }, type) => {
  commit('SET_ENGINE', type);
};

export const setDoc = ({ commit }, type) => {
  commit('SET_DOC', type);
};

export const setCH = ({ commit }, type) => {
  commit('SET_CH', type);
};

export const setNatureList = ({ commit }, type) => {
  commit('SET_NATURELIST', type);
};

export const setNavId = ({ commit }, type) => {
  commit('SET_NAVID', type);
};

export const setCMsg = ({ commit }, type) => {
  commit('SET_CMSG', type);
};

export const setVMsg = ({ commit }, type) => {
  commit('SET_VMSG', type);
};

export const setCArr = ({ commit }, type) => {
  commit('SET_CARR', type);
};

export const setSElems = ({ commit }, type) => {
  commit('SET_SELEMS', type);
};

export const setTElems = ({ commit }, type) => {
  commit('SET_TELEMS', type);
};

export const setHitArr = ({ commit }, type) => {
  commit('SET_HITARR', type);
};

export const setNatureStatus = ({ commit }, type) => {
  commit('SET_NATURESTATUS', type);
};
